# ABOUTME: Rust port of DBuilder.IO (UDB Source/Core/IO and General leaf pieces).
# ABOUTME: First slices: MurmurHash2 and WAD lump name encoding; WAD archive IO follows.

[package]
name = "dbuilder-io"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
