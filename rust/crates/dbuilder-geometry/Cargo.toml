# ABOUTME: Rust port of DBuilder.Geometry (UDB Source/Core/Geometry leaf math).
# ABOUTME: Pure math crate with no dependencies; behavior pinned against the C# tests.

[package]
name = "dbuilder-geometry"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
