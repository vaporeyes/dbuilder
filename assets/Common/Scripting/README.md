Default script editor configuration assets are published from this directory.

The editor resolves script compiler and autocomplete configuration from `Assets/Common/Scripting` relative to the selected bundled configuration root.
