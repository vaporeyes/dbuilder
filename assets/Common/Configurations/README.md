Default game configuration assets are published from this directory.

DBuilder currently locates bundled configuration assets here when they exist, then falls back to a development Ultimate Doom Builder checkout. Platform packages keep this directory so real UDB-compatible configuration files can be added without changing the packaged layout.
