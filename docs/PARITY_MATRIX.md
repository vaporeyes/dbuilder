# UDB Parity Matrix

Status values:

- `ported`: DBuilder has an intentional replacement with meaningful test coverage.
- `partial`: DBuilder has a replacement, but known UDB behavior remains missing.
- `missing`: No meaningful DBuilder replacement exists yet.

## Core

| UDB area | Status | DBuilder location | Notes |
| --- | --- | --- | --- |
| `Source/Core/Actions` | partial | `DBuilder.Editor`, `DBuilder.IO` | Stable command descriptors, begin/end action attributes, shortcut option metadata, keyboard, mouse, and scroll shortcut resolution, configurable shortcut overrides, command hints, repeat suppression, UDB-style Go To Coordinates and Toggle Info Panel action aliases, explicit Properties command binding, flag and Custom Fields command availability rules including selected sidedefs, auto-clear sidedef texture toggle action, Select Single-sided and Select Double-sided action metadata, Filter Selected Things action metadata, Change Map Element Index action metadata, Flip Linedefs and Flip Sidedefs action metadata, Align Linedefs action metadata, Join Sectors and Merge Sectors action metadata, fixed-step sector floor and ceiling height action metadata, sector brightness step action metadata, visual camera movement and Orbit action ids, visual Select, Paint Selection, Edit, Clear Selection, and Toggle Gravity aliases, visual height step action metadata, visual raise/lower to nearest height action metadata, visual brightness step action metadata, visual Match Brightness action metadata, visual thing visibility action metadata, visual Toggle models rendering action alias, visual Insert Item action metadata, visual thing copy, cut, paste selection, rotation, pitch, roll, Look Through Selection, and Apply Camera Rotation action metadata and aliases, visual Select Texture action metadata, visual texture copy and paste action metadata, visual texture flood-fill action metadata, visual texture and thing reset action metadata, visual texture and thing scale action metadata, visual texture offset step action metadata, visual texture offset clipboard action metadata and aliases, visual texture fitting action metadata, visual texture auto-align action metadata, visual unpegged toggle action metadata, visual copy/paste-properties action metadata, visual Toggle Slope action metadata, visual Reset Plane Slope action metadata, visual slope picking action metadata, visual alpha-based texture highlighting action metadata, and menu/toolbar availability synchronization are implemented. Full UDB action manager parity remains incomplete. |
| `Source/Core/Compilers` | partial | `DBuilder.IO` | Nodebuilder execution and compiler/nodebuilder metadata parsing exist. Full discovery, script compiler execution, error parsing, and UI integration remain. |
| `Source/Core/Config` | partial | `DBuilder.IO` | Basic configuration parsing exists, including config includes and recursive structure merges, build defaults, editor behavior metadata, map format flags, UDMF feature toggles, compatibility metadata, render style and flag metadata, thing flag comparison metadata, universal field metadata, things filter metadata, brightness levels, damage type/internal sound/ignored file metadata, make-door defaults, default thing flags, test launch metadata, map bounds, sky flat metadata, light level mode, long texture name support, required archives, static limits with visplane interpolation, script configuration, argument metadata, universal type registry metadata, primitive universal value handlers, random integer and decimal handlers, string universal value handler, enum option, bits, and strings universal value handlers, texture and flat universal value handlers, color universal value handler, angle universal value handlers, thing type and class universal value handlers, thing tag and radius universal value handlers, linedef type and tag universal value handlers, sector effect and tag universal value handlers, polyobject number universal value handler, enum lists and items, generalized categories, options, and bits, thing categories and type metadata, linedef action metadata, sector effects, texture sets, matching texture filters, resource texture sets, linedef activations, flag translations, compiler lists, nodebuilder lists, map lump script-build metadata, texture defaults, default sky textures, texture and flat mixing metadata, and several type sections. Remaining type sections and broader bundled-config parity remain. |
| `Source/Core/Controls` | partial | `DBuilder.Editor` | Avalonia dialogs exist for selected workflows only. Argument property editors and configured universal field rows now use universal handler metadata for boolean, inline enum, named enum, byte-angle, integer degree-angle, decimal degree-angle, and thing-class choices, including the standalone custom-fields dialog, with raw custom-field string preservation and dynamic custom-field type metadata persistence. |
| `Source/Core/Data` | partial | `DBuilder.IO` | Resource manager and data-location metadata exist, including resource-list serialization, duplicate combination, display names, PK3 root texture and flat options, WAD strict patch option handling, config-driven texture and flat namespace mixing, palette lookup, default gray PLAYPAL fallback, nearest PLAYPAL color lookup, ARGB-to-index quantization, indexed image generation, main colormap lookup, named colormap lookup, colormap texture-name enumeration, sprite name enumeration, root PK3 classic texture composition with nested-WAD PNAMES fallback, and cache invalidation on clear/dispose. Full UDB DataManager parity is incomplete. |
| `Source/Core/Dehacked` | partial | `DBuilder.IO` | Patch data model, parser behavior, sprite replacements, and thing catalog merge support exist. Full UDB baseline DehackedData table processing remains. |
| `Source/Core/Editing` | partial | `DBuilder.Editor`, `DBuilder.Map`, `DBuilder.IO` | Selected editing workflows, active-mode select all and invert selection, UDB-compatible grid snap math, dynamic grid sizing, UDB-style grid transform actions, UDB-style stable selection-group action ids and clear-group shortcuts, Select Single-sided and Select Double-sided linedef filtering, Flip Linedefs selected-linedef and sector behavior, Flip Sidedefs selected-linedef behavior, Filter Selected Things selection narrowing, Change Map Element Index single-selection reordering, Align Linedefs selected-linedef and sector behavior, fixed-step selected or highlighted sector floor and ceiling height changes, configured brightness-level changes for selected or highlighted sectors, UDB-style one-shot Paste Special options, Prefabs menu action surface with Insert Previous Prefab runtime command, paste-properties snapshots, selective applier, active-mode menu wiring, options dialog, and highlight fallback, stable file action, Map Options, Preferences, Game Configurations, Test Map, Things Filters setup, Reload Resources, Grid Setup, Snap Selection to Grid, tag statistics, thing statistics, Go To Coordinates, Show Errors, fixed thing scale, and always-show-vertices action ids, UDB-style selected vertex, linedef endpoint, and thing snap-to-grid mutation, UDB-style withdraw-undo and grouped undo behavior, separate undo/redo history clearing, undo/redo selection clearing and UI labels, UI-independent things filter evaluation, configured things filter selection, custom things filter draft write-back, custom filter list management, connected wall texture auto-alignment, and texture-fit offset wrapping exist. Full edit-mode lifecycle and custom things filter editing remain. |
| `Source/Core/General` | partial | `DBuilder.Editor`, `DBuilder.IO` | Settings, launch helpers, recent file/map persistence, missing-file filtering, UDB-style numbered recent menu presentation with long-path trimming, last-used game configuration startup selection, last-used map folder picker selection, active-config new map format selection, editable before/after command settings in map options, and Test Map skill menu modeling exist. Full application orchestration remains. |
| `Source/Core/Geometry` | partial | `DBuilder.Geometry`, `DBuilder.Map`, `DBuilder.IO` | Many helpers are ported, including UDB-style Hermite spline helpers, thing absolute-Z calculation, thing-to-line alignment, sector linedef orientation flipping, sector bounding boxes, point intersection, floor and ceiling planes, sidedef lightfog maintenance, sector fade color selection, flat and wall texture floodfill, explicit-end closest path tracing, traced-side sector joining, traced-side sector creation, loop sector creation, one-sided sector line normalization, marked action and tag tools, config-aware sidedef Y-offset normalization, linedef grid intersections, and triangulation-based sector label positions. Remaining `Tools.cs` and edge behavior need comparison. |
| `Source/Core/GZBuilder` | partial | `DBuilder.Editor` | Main editor shell exists. Full shell behavior remains. |
| `Source/Core/IO` | partial | `DBuilder.IO` | WAD and map IO exist, including static format constraints before saves, config-aware WAD and PK3 embedded-WAD map discovery with map-name-format filtering, collision-safe map sub-lump reads, bounded configured-map-lump find/remove helpers, typed map-lump copying, stream-level UDMF reader and writer facades, UDB-order UDMF element emission, unknown top-level UDMF collection preservation, invalid and zero-length Doom/Hexen/clipboard linedef filtering, Doom-to-Hexen thing flag translation through UDMF, Doom-format conversion argument clearing, Hexen hardcoded line-id action conversion to and from UDMF, UDMF-only element cleanup during binary conversion, configured map-lump ordering during save, configured script and blind-copy lump preservation during save, referenced Doom/Hexen sidedef materialization, invalid and near-zero UDMF linedef filtering, invalid UDMF sidedef filtering, referenced UDMF and clipboard sidedef materialization, UDMF `moreids` normalization, collision-safe map marker renaming, required nodebuilder lump checks, nodebuilder temporary-map cleanup, required config lump creation, duplicate map replacement cleanup, and save-back protection for non-map lumps with map-like names. Full stream, conversion, and save semantics remain. |
| `Source/Core/Map` | partial | `DBuilder.Map`, `DBuilder.IO` | Core map elements exist. Map options, grid setup, persisted grid background scale limits, selection groups, disposal and orphan cleanup, typed field and argument helpers, UDB-style custom UDMF field name validation, quoted string preservation for editable custom fields, tag collections, element lookup helpers, sidedef part semantics, traced-side sector joining, static format constraints, UDB-style blockmap thing-radius indexing, and visual block point, rectangle, line-bound, and frustum queries are ported. Broader map behavior remains partial. |
| `Source/Core/Plugins` | missing | none | Plugin framework not ported. |
| `Source/Core/Properties` | partial | `DBuilder.Editor` | Basic property dialogs exist, including selected vertex, linedef, sidedef, sector, thing, sidedef UDMF flag editing, selected sidedef custom-fields editing, configured universal fields in the standalone custom-fields dialog, and dynamic custom-field type metadata persistence. Full typed property system remains. |
| `Source/Core/Rendering` | partial | `DBuilder.Rendering`, `DBuilder.Editor`, `DBuilder.Map` | Silk.NET renderer exists, with UDB-style classic 2D view mode action ids, renderer switching, persisted default view mode setting for wireframe, brightness, floor textures, and ceiling textures, UDB-style full-brightness display toggling, persisted highlight display toggling for target and mode hover overlays, persisted alpha-based texture highlighting toggle state, visual vertex handle model coverage for floor/ceiling height fallback and handle bounds, UDB-style visual slope handle local meshes and placement basis, UDMF comment icon render planning for linedefs, sectors, and things, UDB-style pixel color and indexed color collection defaults, image brightness color-correction tables, UDB-style dynamic light colors for internal and GLDEFS-backed 2D thing markers, UDB-style dynamic light color tinting for 2D and 3D thing sprite billboards plus live 3D MODELDEF model and KVX voxel mesh batches, UDB-style glowing flat display-state resolution with 2D fullbright surface overrides, UDB-style fullbright, UDMF surface-lighting, and Doom 64 color tints for visual 3D floors, ceilings, resolved 3D floor faces, walls, and ordinary things, and UDB-style thing display-source resolution for MODELDEF models, VOXELDEF voxels, sprites, and marker fallbacks, including 2D and 3D billboard fallback planning, MODELDEF actor sprite-frame eligibility, disabled frame indexes, loader-ready model parts, skins, surface skins, `Skin` override behavior, named frames, frame indexes, transform metadata, loader-facing model data projection, model render mode gating, MODELDEF/thing transform planning, model render mode cycling, UDB-style Toggle models rendering action alias, persistence, menu state, 3D billboard fallback gating, loaded model mesh render-batch planning, transformed model vertex upload preparation, live 3D MODELDEF mesh drawing before billboard fallback, and live 3D KVX voxel mesh drawing before billboard fallback, shared model parser dispatch, Skin and SurfaceSkin texture planning, frame constraint errors, Unreal companion lookup, aggregate bounds and radius calculation, OBJ model parser behavior for geometry, material skins, surface-skin overrides, and parse errors, MD2 model parser behavior for frame selection, coordinate rotation, UV handling, vertex duplication, and parse errors, MD3 model parser behavior for surface geometry, shader skins, surface-skin overrides, normals, grouping, and parse errors, static IQM parser behavior for material meshes, float position/UV/normal arrays, coordinate rotation, and parse errors, KVX voxel parser behavior for slab offsets, visible face generation, pivot transforms, palette atlas pixels, radius calculation, and malformed input errors, and Unreal _a.3d/_d.3d parser behavior for frame selection, packed and Deus Ex vertices, texture groups, UVs, masked polygons, flat normals, and frame errors. Full render pipeline and visual mode rendering remain. |
| `Source/Core/Resources` | partial | `DBuilder.IO` | WAD, PK3, nested PK3, directory, mixed stack, texture, flat, and sprite resource behavior has focused coverage, with editor add-resource options for WAD, PK3, directory resources, required archives, required-archive default detection, MODELDEF model resource lookup, UDB-style supported model skin texture extension probing, and defined texture, basename texture, and sprite fallback for model skin images. Full DataManager, lazy loading, cache invalidation, hires, model, voxel, and advanced namespace behavior remains. |
| `Source/Core/Types` | partial | `DBuilder.IO` | Generalized types and game config subsets exist. Full type manager remains. |
| `Source/Core/VisualModes` | partial | `DBuilder.Editor`, `DBuilder.Map` | Picking and 3D helpers exist, including visual vertex handle floor/ceiling pair modeling, explicit height-offset state, adjacent-sector height fallback, visual slope sidedef and vertex handle placement and pivot models, UDB-style slope handle height-change plane application, visual Toggle Slope Plane_Align mutation for selected surfaces, selected floor and ceiling visual slope reset behavior, runtime visual slope picking mode toggles with UDMF and plane-equation support guards, persisted adjacent vertex-slope selection state, persisted alpha-based texture highlighting toggle state, UDB-style held visual camera movement and target-centered Orbit action ids, visual Select, Edit, Clear Selection, and Toggle Gravity aliases, double-sided middle texture alpha picking, 3D floor flat alpha picking, 3D floor side-band picking, sloped 3D floor plane picking, target-sector and sidedef ownership for 3D floor side hits, Move Camera to Cursor placement, visual floor, ceiling, and thing height, raise/lower to nearest height, and brightness step actions, visual Match Brightness dispatch for UDMF surface light fields, visual Insert Item thing-at-target dispatch, visual thing copy, cut, paste selection, rotation, pitch, roll, Look Through Selection, and Apply Camera Rotation dispatch aliases, visual Select Texture dialog dispatch, visual texture copy and paste dispatch, visual texture flood-fill actions, visual texture and thing reset actions, visual wall texture scale actions, visual wall texture offset step actions, visual wall texture offset copy and paste dispatch aliases, visual copy/paste-properties target dispatch with options dialog routing, visual selected wall texture fitting actions, visual texture auto-align actions, visual upper and lower unpegged toggle actions, visual thing relative movement, cursor placement, scale, visibility cycling, and Align Things to Nearest Linedef action alias, thing-radius, rectangular, line-bound, and frustum blockmap queries, visual culling deduplication for visible lines and things, and culled 3D thing billboard drawing. Full visual-mode framework remains. |
| `Source/Core/Windows` | partial | `DBuilder.Editor` | Selected windows exist, including thing, texture, flat, action and effect browsers, preferences, find/replace, open-map selection with active-config candidate filtering, sidecar game-configuration loading, script-compiler override/default selection, strict-patch toggling, and config-aware long-texture-name toggling, game configuration selection with active-row matching and per-configuration resource editing and persistence, basic map options including compiled ACS script compiler selection, resource options with tested UDB-compatible option mapping, grid setup with named background source choices, percent background scale fields, background texture and flat browsers, background image file picking, show-background toggling and disabled-state behavior, go-to-coordinates, map check results with clipboard and selected-type operations, tag and thing statistics, selected sidedef custom-fields editing with configured universal field rows and dynamic field type persistence, structured single-element info panels including sidedef rows and previews, info-panel collapse and expand command behavior, exception and About dialogs. Full UDB dialog set remains. |
| `Source/Core/ZDoom` | partial | `DBuilder.IO` | Several parsers exist, including ANIMDEFS, DECORATE/ZScript actor metadata, MODELDEF with transform, actor pitch/roll, rotation-center, named frame metadata, duplicate frame suppression, UDB-style invalid model resource path character rejection, UDB-style empty `Path` rejection, and orientation-flag recovery after unknown MODELDEF directives, SNDINFO, SNDSEQ, TERRAIN, LOCKDEFS, DECALDEF, VOXELDEF, IWADINFO, CVARINFO, REVERBS, X11 RGB, TEXTURES metadata, GLDEFS wall and flat glow metadata, and MAPINFO numbered actor discovery. Full actor semantics, model rendering, voxel rendering, and Dehacked merge behavior remain. |
| `Source/Native/OpenGL` | partial | `DBuilder.Rendering` | Replaced with Silk.NET abstraction. Full parity not established. |

## Bundled Plugins

| UDB plugin | Status | DBuilder location | Notes |
| --- | --- | --- | --- |
| `3DFloorMode` | partial | `DBuilder.Map`, `DBuilder.IO` | 3D floor data helper exists, including selected-sector discovery, control-sector edit snapshots, control-sector mutation, orphan action-line cleanup, mode/action metadata, command catalog entries, control-sector area settings, custom tag range allocation, control-sector area placement planning, control-sector geometry materialization, action-line binding, slope vertex group persistence, slope data-sector fields, floor/ceiling plane application, selected slope vertex editing, sector plane binding helpers, draw-slope acceptance, group allocation, selected-sector binding, and data-sector persistence. Full plugin workflow, editor window, save-back wiring, slope drawing UI, and slope editing UI remain missing. |
| `AutomapMode` | partial | `DBuilder.Editor`, `DBuilder.Map` | Automap line visibility, color presets, line classification, secret-sector and lock overlays, format-aware UDMF/classic automap flags, selected line/sector flag toggles, mode metadata, persisted setting defaults, presentation layer descriptors, valid-linedef collection, highlight target planning, render planning for line draw order, highlight colors, and textured-background fallback, editor Automap Mode rendering with UDB-style valid-line colors and textured-sector visibility, hover highlighting, click toggles for highlighted line and sector automap flags, and toolbar-backed persisted show-hidden-lines, show-secret-sectors, show-locks, show-textures, and color-preset options exist. Full plugin lifecycle and docked workflow remain missing. |
| `BlockmapExplorer` | partial | `DBuilder.Editor`, `DBuilder.Map`, `DBuilder.IO` | Blockmap logic, overlay toggle, edit-mode metadata, parsed BLOCKMAP highlighted-block/shared-list/questionable-offset overlays, and a non-modal explorer summary window exist. Full mode workflow remains missing. |
| `BuilderEffects` | partial | `DBuilder.Map`, `DBuilder.Editor` | Selected effects exist, including Randomize, Apply Directional Shading, and OBJ terrain import command metadata, dispatch, menu/toolbar action surface, visual-mode Randomize selection routing for selected things, flats, and walls, Randomize sector floor and ceiling offset-mode dialog controls, upper and lower unpegged flag controls, upper and lower texture fill controls, remembered jitter and directional shading dialog settings, triangular UDMF sector vertex-height jitter, nearest non-incident linedef safe-distance clamping and selected-endpoint propagation for vertex translation jitter, half-sector-span safe-distance clamping for sector height jitter, Doom-angle snapping for Randomize thing rotations when configured, nearest-thing safe-distance clamping for thing translation jitter, thing-height jitter when the map format supports thing heights, UDMF thing pitch, roll, relative/negative pitch and roll, scale, and negative-scale jitter, UDB-style OBJ terrain import up-axis, scale, vertex-height settings, non-UDMF vertex-height thing fallback, imported linedef sidedness flags, and visual-mode Apply Directional Shading routing for selected floors and walls. Full plugin missing. |
| `BuilderModes` | partial | `DBuilder.Editor`, `DBuilder.Map` | Selected drawing/editing tools, make-sector-at-cursor, draw-sector, rectangle, ellipse, and draw-grid sector materialization through UDB-style traced sector creation, draw-mode continuous accept lifecycle, draw-mode thing placement from generated rectangle, ellipse, and curve vertices, Place Things from selected vertices, linedefs, and sectors, Filter Selected Things action, Change Map Element Index action, UDB-style paste-properties option tab modeling, snapshot clipboard, selective property application, editor menu command wiring, and visual target copy/paste-properties dispatch, selected thing align-to-wall and point-to-cursor commands, selected-sector floor, ceiling, brightness, UDMF floor/ceiling light, and light/fade color gradient commands, UDMF selected-linedef brightness gradient command, fixed-step selected or highlighted sector floor and ceiling height actions, configured brightness-level step actions, visual floor, ceiling, and thing height and brightness step actions, visual thing visibility cycle action, visual texture flood-fill actions, visual texture and thing reset actions, visual texture and thing scale actions, visual wall texture offset step actions, visual wall texture offset copy and paste actions, visual selected wall texture fitting actions, visual texture auto-align actions, visual upper and lower unpegged toggle actions, visual Toggle Slope Plane_Align mutation, visual Reset Plane Slope mutation, visual slope picking mode toggles with UDMF and plane-equation support guards, visual double-sided middle texture alpha picking, visual 3D floor flat alpha picking, visual 3D floor side-band picking, visual sloped 3D floor plane picking, visual 3D floor side hit target-sector and sidedef ownership, persisted adjacent vertex-slope selection state, visual alpha-based texture highlighting toggle state, Split Linedefs action, Select Single-sided and Select Double-sided linedef actions, Flip Linedefs action, Flip Sidedefs action, Align Linedefs action, Join Sectors and Merge Sectors command actions, Apply lightfog flag action, gradient interpolation mode selection, selected-linedef curve materialization, edit-selection arbitrary rotation, rotation snapping, sector height adjustment, non-uniform scaling, mirrored-linedef flipping, undo/redo panel timeline modeling, and non-modal undo/redo panel UI, Make Door sector mutation behavior, options dialog, persisted dialog defaults, and mixed-floor default handling, export planning models, legacy object export geometry writing with editor command integration, image export PNG and brightmap rendering helpers with editor command integration, Wavefront ordinary and resolved 3D-floor visual-sector geometry collection and editor OBJ export command, idStudio dialog defaults and texture-count labels, visual vertex slope smart pivot and adjacent-handle selection helpers, visual slope command metadata, select-similar property matching, active-mode menu command, and options dialog, within-selection find/replace scoping, UDB-style replace availability, generalized action and sector-effect find/replace matching, focused map error result fixes, UDB Map Analysis mode descriptor, lifecycle, redraw planning, and short-linedef zoom metadata, selected-result description text for short, overlapping, and line-reference linedef results, action-driven lower texture checks, 3D floor texture requirements, Plane_Align missing-texture suppression, Static_Init sky-transfer texture handling, UDMF flat alignment to selected linedef sides, required-slot texture alignment checks, UDB-style sky-flat upper texture suppression, context-independent fractional-coordinate off-grid checks, UDMF-only zero-length-inclusive short-linedef checks, UDB map-size, overlapping linedef, empty/unclosed/invalid sector, stray and overlapping vertex, missing-sidedef and sidedness mismatch, missing/unknown/unused/misaligned texture and flat, missing-activation, unknown linedef-script, unknown action/effect, stuck-thing, unknown/obsolete/unused thing, and polyobject diagnostics, ignored error-check suppression, and selected-result ignore, show-all, and type-filter UI exist. Full mode set remains missing. |
| `ColorPicker` | partial | `DBuilder.Editor`, `DBuilder.Map` | RGB/HSV conversion, picker formatting, typed HEX and float color parsing, packed UDMF color integers, sector light/fade color fields, selected-sector color command with UDMF guard, separate lightcolor/fadecolor field drafts, UDB-style sector dialog title, sector-color label, typed HEX and float input fields, paired-field apply behavior, dynamic light picker metadata, color storage, slider labels, control visibility, slider limits, fixed-value capture, selection-wide color mutation, radius/interval mutation rules, and Color Picker toggle routing, warning text, and editor command/dialog wiring with availability gating for selected UDB internal dynamic light things exist. The editor Color Picker toggle now routes through the UDB-style mode and selection decision model. Full picker controls and GLDEFS actor-light editor wiring remain missing. |
| `CommentsPanel` | partial | `DBuilder.Editor`, `DBuilder.Map` | Comment grouping, current-mode filtering, set/remove helpers, selection targets with sector boundary propagation, edit targets with sidedef-to-linedef conversion, padded view-area calculation, UDB-style comment icon render planning, toggle command metadata, UDB-style edit-object menu and remove-undo labels, and a non-modal comments panel exist. Full docked UI and context menu remain missing. |
| `ImageDrawingExample` | partial | `DBuilder.Editor` | Built-in image-example mode renders an overlay-only screen-space image, with UDB-style plugin, mode, action, lifecycle, overlay presentation, and screen-space redraw metadata. Full sample plugin loading remains missing. |
| `NodesViewer` | partial | `DBuilder.Editor`, `DBuilder.IO` | Classic node data parsing, classic subsector polygon clipping and hit testing, edit-mode metadata, editor subsector overlay rendering, ZNODES payload extraction, UDB-style tree depth and balance stats, and a non-modal Nodes Viewer summary window exist. Full ZNODES structure parsing and full visualization UI remain missing. |
| `RejectExplorer` | partial | `DBuilder.Editor`, `DBuilder.IO` | REJECT validation, UDB-style engage/cancel warnings, visibility relation classification, default overlay colors, relation color mapping, edit-mode metadata, color configuration UI, highlighted-sector overlay rendering, and a non-modal Reject Explorer summary window exist. Complete mode workflow remains missing. |
| `SoundPropagationMode` | partial | `DBuilder.Editor`, `DBuilder.Map` | Sound propagation domain, affected-sector, hunting thing, leak path search, edit-mode metadata, default overlay colors, rotating domain palette helpers, UDB-style plugin lifecycle reset metadata, UDMF `blocksound` reachability, format-aware linedef sound-blocking toggles, highlighted-domain sector overlay rendering, red leak path overlay rendering from two selected sectors, Sound Environment thing grouping with zone-boundary, dormant, name, id, and color rules, color configuration UI, and color configuration action, label, reset, and plugin-setting metadata exist. Full mode UI, Sound Environment docker UI, and plugin workflow remain missing. |
| `StairSectorBuilder` | partial | `DBuilder.Map`, `DBuilder.Editor` | Stair builder helper, dialog, option application, straight linedef geometry planning, connected single-step straight stair planning, distinct-sector post-line planning and materialization, curved stair geometry planning, Catmull-Rom spline stair geometry planning, straight stair sector materialization, UDB-style Stair Sector Builder mode and Select Sectors Outline command metadata, outline selection helper, prefab settings key model, prefab collection settings shape, numeric prefab settings load ordering, prefab name suggestion, reserved-name, overwrite and insert-position behavior, form startup state for default prefabs, available tabs, selected-sector overrides, form placement metadata, and loaded-prefab option mapping exist. Editor prefab UI persistence and preview workflow remain missing. |
| `TagExplorer` | partial | `DBuilder.Editor`, `DBuilder.IO` | Tag entry filtering, sorting, grouped tree shaping, thing category grouping in index view, configured action and effect labels in action grouping, generalized sector effect expansion in action grouping, export text shaping, grouped non-modal Tag Explorer view, text export command, UDB-style rebuild selection and export state, and UDB-style docker, control-label, tooltip, export, plugin-setting, and delayed refresh lifecycle metadata exist. Full docked live UI behavior remains missing. |
| `TagRange` | partial | `DBuilder.Editor`, `DBuilder.Map` | Absolute and relative tag range calculation, duplicate and skip-used handling, selected assignment, persisted step and relative-mode defaults, UDB-style form title, end-tag preview and warning state, action, toolbar, dialog label, warning, undo text metadata, toolbar update lifecycle plan, and editor dialog integration exist. Full plugin lifecycle and docked workflow remain missing. |
| `UDBScript` | partial | `DBuilder.IO`, `DBuilder.Editor` | UDBScript action metadata, execute-current and execute-slot dispatch planning, preferences metadata, external editor persistence planning, editor settings external-editor field, external editor fallback resolution, external editor launch planning, and editor command catalog entries exist for Execute Script and 30 script slots. Script directory discovery, script watcher reload filtering, script load retry policy, script load retry application, hidden-folder skipping, JS file filtering, UDB-style ASCII path hashing, leading template metadata parsing, option config parsing, valid option type checks, invalid option type skipping, enum default mapping, option setting-key generation, saved-value loading, UDB-style save/delete operation planning, runner lifecycle text, script engine setup option and binding planning, feature-version gate text, feature-version continue and ignore-version branch planning, feature-version confirmation dialog and runner prompt hook, runtime formatting, runtime constraint prompt metadata, runtime constraint abort and restart branch planning, runtime constraint confirmation dialog and runner prompt hook, runner UI lifecycle state, runner form run workflow planning, editor runner finish workflow plan wiring, editor runner start run-request wiring, runner start and action-button branch planning, runner load and closed lifecycle event planning, editor runner timer startup wiring, editor runner cancellation token wiring, editor runner timer threshold action-button wiring, runner timer tick branch planning, runner InvokePaused timer-pause planning, editor runner InvokePaused stopwatch wiring, runner RunAction branch planning, editor runner RunAction plan wiring, runner form designer metadata, editor runner window shell, runner status callback state and window API, runner callback invocation window API, runner window progress style state, runner window autoclose state tracking, runner progress update branch planning, editor runner progress update plan wiring, runner progress/log visibility and autoclose state, runner pre-run and post-run side-effect planning, runner visibility threshold, timer interval, elapsed title formatting, log formatting, error dialog metadata, script error dialog editor surface and runner prompt hook, exception handling branch planning, library import exception branch planning, script-version member access filtering, source import planning, run file execution and stopwatch sequence planning, docker labels, docker layout metadata, editor docker window shell, editor docker command and menu integration, docker context-menu metadata, docker folder expansion state, docker folder expansion persistence, docker folder expansion settings load, docker tree reselection, docker tree sorting and filtering, docker selection metadata, docker selection transition, docker option reset, docker option reset editor apply wiring, slot menu text, slot assignment behavior, slot settings load and save planning, QueryOptions add and clear behavior, QueryOptions enum default mapping, QueryOptions value collection, QueryOptions API member surface metadata, QueryOptions enum-value container gating, QueryOptions returned-value handler conversion, QueryOptions dialog layout metadata, QueryOptions option row metadata, QueryOptions prompt UI metadata, QueryOptions paused query result planning, QueryOptions editor dialog and runner prompt hook, script options grid UI metadata, script options editor dialog surface, script option value-cell reset and style metadata, script option enum editor state metadata, script option cell-begin-edit branch metadata, script option browse-button state metadata, script option selection-change branch metadata, script option enum-editor mouse-up branch metadata, script option enum validating branch metadata, script option cell-click branch metadata, script option end-edit branch metadata, script option browse refresh branch metadata, script option cell-end-edit commit metadata, script option browse-click branch metadata, script option enum apply state metadata, script option value collection, API vector conversion helpers, Vector2D and Vector3D wrapper arithmetic, geometry, mutable-coordinate, and vector-like equality helpers, Line2D wrapper geometry helpers, Angle2D wrapper conversion and angle helpers, Plane wrapper geometry helpers, GameConfiguration wrapper metadata access, map element arguments wrapper access, Vertex wrapper core property, distance, connected-linedef, nearest-linedef, deletion, and join access, Vertex and Thing wrapper snap-to-accuracy, UDB-style snap overload, and grid snapping access, Linedef wrapper core property, geometry, multi-tag, sided-flag application, split, and deletion access, Sidedef wrapper core texture, offset, highlighted-part, and link access, Sector wrapper core property, highlighted-surface, slope, selected-linedef synchronization, tag, sidedef, label-position, triangle, deletion, and join access, Thing wrapper core property, managed scale field, position, call-time sector determination, index-based string formatting, and deletion access, Map wrapper core element enumeration, map-format, mouse-position, and visual-camera access, Map wrapper tag allocation and nearest element query access, Map wrapper marks clear, invert, query, and selected-mark access, Map wrapper selected, highlighted, selected-or-highlighted, selected-sidedef, clear-selected, and grid snapping access, Map wrapper vertex, thing, and draw-lines creation access, Map wrapper sector join, merge, and stitch-geometry access, Map wrapper snap-all-to-accuracy and UDB-style snap overload access, BlockMap wrapper options-object construction, entry, query result, and cached wrapper access, Data and ImageInfo wrapper texture and flat metadata access, map element custom UDMF field and active-flag rejection access, map element wrapper index access, map element named and classic numeric flag mutation access, including classic map-format propagation through nested sidedef and sector wrappers, API universal-value conversion helpers, legacy/global binding metadata, top-level host API property metadata, top-level host progress, logging, message, exit, and die behavior, top-level host message dialog metadata, top-level host MessageForm UI metadata, top-level host message dialog and runner prompt hook, and exception outcome classification exist. Script engine execution, option UI, editor-applied option persistence, docker UI integration, preferences UI integration, full runner execution wiring, full runtime constraint integration, full QueryOptions runner UI integration, and full API wrappers remain incomplete. |
| `USDF` | partial | `DBuilder.Editor`, `DBuilder.IO` | DIALOGUE parsing, include lump resolution from current map WADs and embedded PK3 map archives, dialog editor action/tool metadata, UDB-style plugin lifecycle plan, UDB-style window setting keys, Dialog Editor form title, tree metadata, image-key metadata, and a non-modal conversation window exist. Editing and save-back remain missing. |
| `VisplaneExplorer` | partial | `DBuilder.Map`, `DBuilder.IO` | UDB-style tile sampling, packed point-stat storage, stat compression, heatmap byte interpolation, palette rows, heatmap palette selection, void-color override, tile coordinate flooring, view-prioritized scan queueing, map-bound tile creation with one-sided outside-tile culling, NODES preflight checks, tile hover value labels, custom view-height input normalization, mode/action metadata, toolbar settings state for open doors, heatmap, and view height, stat dropdown item metadata, toolbar text and tooltip metadata, toolbar add/remove lifecycle plan, view-height preset item metadata, overlay rectangle planning, and editor overlay rendering exist. Native VPO execution and full UI remain missing. |
| `WadAuthorMode` | partial | `DBuilder.Map`, `DBuilder.Editor` | Hover highlight priority, highlight transition planning, mode lifecycle planning, renderer-scale ranges, side-based sector selection, linedef popup command order, tool form and linedef popup metadata, mode enter/leave selection behavior, edit-mode descriptor, `wadauthormode` action metadata, editor menu and toolbar toggle, hover highlight rendering, and linedef popup execution for properties, delete, split, flip, and default curve materialization exist. Full plugin workflow remains missing. |
